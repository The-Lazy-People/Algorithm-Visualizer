<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thelazypeople.algorithmvisualizer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".kmp.KmpActivity"></activity>
        <activity android:name=".nQueen.ShowSolnActivity" />
        <activity android:name=".nQueen.VisualizationActivity" />
        <activity android:name=".nQueen.nQueenActivity" />
        <activity android:name=".pathfinder.TutorialActivity" />
        <activity android:name=".pathfinder.PathFinderActivity" />
        <activity android:name=".searching.SearchingActivity" />
        <activity android:name=".sorting.SortingActivity" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>